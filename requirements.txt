# ==============================================================================
# EP PLN - Classificação de Estilos Textuais (TF-IDF, MLP, BERT)
# ==============================================================================

# Manipulação de dados
pandas>=1.3.0
numpy>=1.21.0

# Machine Learning
scikit-learn>=1.4.2  # TF-IDF, LogisticRegression, GridSearchCV

# Visualização
matplotlib>=3.5.0
seaborn>=0.11.0

# Explicabilidade
eli5>=0.13.0  # Análise de features importantes

# Deep Learning - TensorFlow/Keras (MLP)
tensorflow==2.18.0  # TensorFlow 2.18.0
tf-keras==2.18.0 
scikeras>=0.13.0  
# Deep Learning - PyTorch (BERT)
# IMPORTANTE: Para instalar PyTorch 2.6+ a GPU NVIDIA, use:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# 
# Para instalar sem GPU (CPU apenas):
#   pip install torch
#
# Versão mínima: 2.6.0 (necessário para resolver vulnerabilidade CVE-2025-32434)
torch>=2.6.0

# Transformers (BERT)
transformers>=4.30.0  #
datasets>=2.10.0 
tokenizers>=0.13.0 

# Jupyter
jupyter>=1.0.0
notebook>=6.4.0
ipython>=7.0.0

# Word Embeddings (Word2Vec)
gensim>=4.3.0

# Utilitários
tqdm>=4.65.0  
requests>=2.28.0  
